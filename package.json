{
  "name": "jsonary-dev",
  "description": "Jsonary development setup",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "grunt": "~0.4.1",
    "wrench": "~1.5.1"
  },
  "scripts": {
    "test": "grunt test",
    "install": "pushd node-package; npm install; popd;"
  },
  "dependencies": {
    "grunt-contrib-compress": "~0.5.2"
  }
}
